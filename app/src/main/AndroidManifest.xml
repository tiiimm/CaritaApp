<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dreamers.caritaapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_carita"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_carita_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.AdvertisementActivity"></activity>
        <activity android:name=".activity.CompanyActivity" />
        <activity
            android:name=".activity.SetUpActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity
            android:name=".activity.HomeActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity
            android:name=".activity.SignupActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity
            android:name=".activity.LoginActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity
            android:name=".activity.SplashScreenActivity"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-5648218939145202~3818614870" />
    </application>

</manifest>